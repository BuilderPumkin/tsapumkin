# OPERATIONS & ROLLBACK
## Feature Flag Control
Set `window.PUMKIN_DEEP_LEARNING_ENABLED = false` to disable the integration globally.
## Emergency Teardown
Invoke `PumkinDeepLearningIntegration.safeFallbackToStandardPractice()` during runtime.
