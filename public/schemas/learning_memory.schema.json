{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://pumkin.dev/schemas/learning_memory.schema.json",
  "title": "LearningMemorySignal",
  "description": "Structured educational signals for long-term memory aggregation",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "description": "Unique UUID for the signal"
    },
    "skill_id": {
      "type": "string",
      "description": "Target skill, topic, or concept node"
    },
    "signal_type": {
      "type": "string",
      "enum": ["error", "mastery", "hint_request", "concept_review", "struggle"],
      "description": "The pedagogical category of the signal"
    },
    "value": {
      "type": ["string", "number"],
      "description": "Contextual value (e.g., 'calculation_error', or hint level requested: 3)"
    },
    "confidence": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "How certain the system is about this signal (1.0 = explicit user action, 0.8 = inferred)"
    },
    "source": {
      "type": "string",
      "enum": ["exam_engine", "ai_tutor", "manual_review"],
      "description": "Which sub-system generated this signal"
    },
    "timestamp": {
      "type": "string",
      "format": "date-time"
    }
  },
  "required": ["id", "skill_id", "signal_type", "value", "confidence", "source", "timestamp"]
}
